# .cursorrules 文件
本文件用于记录实际项目文件和代码操作的变更记录，必须与实际文件和代码操作同步更新，所有操作必须记录在.cursorrules文件中.

## 同步规则
1. **项目背景**：
   - 项目目标必须与实际开发内容一致
   - 项目范围必须列出所有主要功能模块
   - 功能模块的变更必须同步更新到未实现功能列表

2. **运行环境配置**：
   - 所有配置必须与实际部署环境保持一致
   - 配置变更必须记录在修改文件记录中
   - 依赖包变更必须更新requirements.txt

3. **编码标准**：
   - 代码风格必须在IDE配置中实施
   - 命名规范必须在代码审查中执行
   - 规范变更必须通知所有开发者

4. **首选库**：
   - 库的版本必须与package.json/requirements.txt一致
   - 新增依赖必须更新到首选库列表
   - 必须说明选择该库的原因

5. **文件结构**：
   - 必须与实际项目目录结构完全一致
   - 新建目录必须说明用途
   - 必须反映最新的项目组织方式

6. **文档规范**：
   - 所有新代码必须遵循文档规范
   - API文档必须与实际接口同步
   - 注释规范必须在代码中得到执行

7. **错误处理**：
   - 错误码必须在文档中定义
   - 日志记录必须遵循配置要求
   - 错误处理机制必须在代码中实现

8. **文件变更记录**：
   - 所有文件的创建、修改、删除操作必须记录在变更记录中
   - 新增的文件夹和文件必须同步更新到文件结构中
   - 删除的文件夹和文件必须从文件结构中移除
   - 每次变更必须更新版本号
   - 必须说明变更原因

9. **项目下一步**：
   - 已实现功能必须与实际功能一致
   - 未实现功能必须定期更新
   - 开发计划

## 项目背景
- **项目目标**：  
  （在此描述项目目标）

- **项目范围**：  
  （在此描述项目范围）

## 运行环境配置
- **基本环境**：
  - 项目名称: 
  - 项目端口: 
  - 域名: 
  - 运行环境版本: 

- **服务器配置**：
  - 启动方式: 
  - 项目路径: 
  - 框架: 
  - 入口文件: 
  - Worker数: 
  - 线程数: 

- **数据库配置**：
  - 类型: 
  - 数据库名: 
  - 主机: 
  - 端口: 
  - 初始化：
    - 初始化方式: 
    - 初始化文件路径: 
    - 执行方式: 

- **日志配置**：
  - 访问日志: 
  - 错误日志: 
  - 日志级别: 

- **依赖管理**：
  - 依赖管理文件: 
  - 镜像源: 
  - 安装命令: 

## 编码标准
- **代码风格**：  
  （在此定义代码风格）

- **命名规范**：  
  （在此定义命名规范）

## 首选库
| 库名称 | 版本 | 用途 |
|--------|------|------|
|        |      |      |

## 文件结构
- **前端**：
  - /client: 前端应用
  - /common: 共享工具和配置
  - /docs: 项目文档

- **后端**：
  - /server: 后端服务
  - /server/database: 数据库文件目录

## 文档规范
- **代码注释**：  
  （在此定义注释规范）

- **API文档**：  
  （在此定义API文档规范）

## 错误处理
- **错误码定义**：
  （在此定义错误码范围）

- **日志记录**：
  （在此定义日志记录规范）

- **错误处理机制**：
  （在此定义错误处理机制）

## 文件变更记录
### 创建文件
| 序列号 | 文件名称 | 文件描述 |
|-------|----------|----------|
|       |          |          |

### 修改文件
| 序列号 | 文件名称 | 修改内容 |
|-------|----------|----------|
|       |          |          |

### 删除文件 
| 序列号 | 文件名称 | 删除原因 |
|-------|----------|----------|

### 版本变更
| 版本号 | 变更说明 |
|-------|----------|
| x.x.x |          |

## 项目下一步
### 已实现功能
- （已实现功能列表）

### 未实现功能
- （未实现功能列表）

### 开发计划
1. （开发计划）